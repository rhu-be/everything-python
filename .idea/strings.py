#strings =
#the followong are functiosn in strings:
# length = len(name)
# index = name.find(" ")- used to find spaces in a string.
# name = name.capitalize()
# name = name.upper()
# name = name.lower()
# result = name.isdigit()
# result = name.isalpha()
# result = phone_number.count(" ")
# phone_number = phone_number.replace("-", "")
# string index start at zero hence-


def introduction(name):
    #capitalization
    name = name.title()
    #length of the characters
    length = len(name)

    print(f"{name} is your name and it has {length} characters")

user = input("Enter your name: ")
introduction(user)

#reversing a string
 def title(book):
     

